# 2026-03-13

## opencode-plan-todo npm Plugin 改造 (v0.2.0)

- 将 `opencode-plan-todo` 从纯 Markdown 配置层改造为 OpenCode npm Plugin 包
- 创建 `package.json`, `tsconfig.json`, `.npmignore`, `src/index.ts`, `src/deploy.ts`
- Plugin 实现：启动时自动部署 agents/commands/templates + 事件钩子
- 新增 `scripts/install.sh` bash 安装脚本
- 更新 README、CHANGELOG、docs/installation.md、docs/upgrade-compatibility.md
- 配置仓库 git 身份：spartawhy117 / 864290826@qq.com
- 推送到 GitHub: https://github.com/spartawhy117/opencode-plan-todo (commit f5c8358)
- 向 OpenCode 官方 ecosystem 提交 PR: https://github.com/anomalyco/opencode/pull/17296
- **注意**：当前 shell 环境是 CMD（非 PowerShell），git commit message 含空格时需用脚本方式或避免引号
- **GitHub CLI** 已认证（spartawhy117, HTTPS, keyring token）
- PR #17296 被 bot 标记为缺少模板部分，使用 `gh api --method PATCH` 按照官方 PR 模板重新填写了描述（Issue/Type/What/Verify/Screenshots/Checklist 全部章节）

## 重命名 + 清理 + Release v0.2.1

- 项目重命名：`opencode-plan-todo` → `opencode-enhance-plan`
- GitHub 仓库通过 API 重命名：https://github.com/spartawhy117/opencode-enhance-plan
- 全面更新：package.json、README.md、CHANGELOG.md、src/index.ts、docs/*、scripts/*、templates/*、.npmignore
- `docs/feature-lifecycle.md` 移至 `legacy/`（内容已在 agent 定义中完整存在）
- 清理临时文件：`pr_body.json`、`update_pr.ps1`、`dist/`
- 发布 Release v0.2.1：https://github.com/spartawhy117/opencode-enhance-plan/releases/tag/v0.2.1
- PR #17296 已关闭（旧名称），新 PR #17305 已按模板提交：https://github.com/anomalyco/opencode/pull/17305
- 本地 git remote 已更新为新仓库 URL
- **git commit 注意**：CMD shell 中 `git commit -m` 经常 exit code 1 无输出，需用 `git -C <path> commit -F <file>` 方式

## 修复 CI 依赖问题 + Release v0.2.2

- **根因**：`package.json` 的 `devDependencies` 中声明了 `zod`，但代码中从未 import/使用
- `zod` 在 OpenCode CI 的 `bun install --no-cache` 环境下触发 `ENOENT: failed opening cache/package/version dir for package zod`
- **修复**：从 `devDependencies` 移除 `zod`，用 `npm uninstall zod` 确保 lockfile 也清理干净
- 版本升级到 v0.2.2，更新 CHANGELOG
- 发布 Release v0.2.2：https://github.com/spartawhy117/opencode-enhance-plan/releases/tag/v0.2.2
- 关闭旧 PR #17305，重新提交新 PR #17308：https://github.com/anomalyco/opencode/pull/17308
- 新 PR 基于同步后的最新 dev 分支，分支名 `docs-enhance-plan-v2`

## 项目维护 + Release v0.2.3

- 创建 `temp/` 目录用于存放可复用的 PR 工作流脚本
- `.gitignore` 配置 `temp/*` + `!temp/.gitkeep`（跟踪目录，忽略内容）
- `.npmignore` 添加 `temp/`（与 `files` 白名单双重保护，不会发布到 npm）
- 清理根目录遗留临时文件（eco_log.txt, release_notes.md）
- 全面代码审查确认插件实现与 `@opencode-ai/plugin` v1.2.25 API 完全匹配
- 发布 Release v0.2.3：https://github.com/spartawhy117/opencode-enhance-plan/releases/tag/v0.2.3
- 关闭旧 PR #17308，提交新 PR #17316：https://github.com/anomalyco/opencode/pull/17316
- PR #17316 CI 结果：10/11 通过，仅 `e2e (windows)` 失败 — 是 bun#19936 已知问题（`--no-cache` + Windows = ENOENT）
- 关闭 PR #17316，重新提交 PR #17319：https://github.com/anomalyco/opencode/pull/17319
- 新 PR 基于分支 `docs-enhance-plan-v4`（与 v3 相同提交 `5506da9`）
- 在 PR #17319 评论区添加了 e2e (windows) 失败说明：docs-only PR，与 bun#19936 已知问题无关
- 临时 fork 克隆在 `D:\PersonalProjects\opencode-fork-temp`（用户选择保留）
- **CMD shell 中 `gh api --method POST -f` 参数方式可能失败**，用 `--input <file>.json` + `write_to_file` 创建 payload 文件更可靠

## 品牌统一 + 安装方式收敛 v0.2.4

- **路径 B 全量执行**：运行时 agent 名称从 `plan-todo` 统一为 `enhance-plan`
- Agent 文件重命名：`agents/plan-todo.md` → `agents/enhance-plan.md`
- 所有 commands frontmatter 更新：`agent: plan-todo` → `agent: enhance-plan`
- 所有 commands 内部文案（feature-switch.md, init-plan.md）中的 `plan-todo` 引用更新
- 文档全面更新：README.md、docs/installation.md、docs/usage.md、docs/upgrade-compatibility.md
- 模板更新：AGENTS.template.md、README.template.md
- 移除旧安装方式：删除 docs 中手动安装/脚本安装章节，删除 README 中手动安装链接和 scripts/ 目录说明
- 删除已废弃脚本：`scripts/install.ps1`、`scripts/install.sh`
- CHANGELOG.md 新增 v0.2.4 条目，含迁移说明
- package.json 版本升级到 0.2.4
- **唯一保留 plan-todo 的地方**：CHANGELOG.md 中的历史版本记录（v0.1.0、v0.2.1 等），属于事实记录不修改

## 升级兼容文档精简

- `docs/upgrade-compatibility.md` 从双语长文压缩为一页"Upgrade Checklist / 升级检查清单"
- 内容聚焦三块：什么时候看、升级后检查什么（5 项表格）、出问题怎么办
- 去掉中英分离的冗余结构，合并为中英混排的简洁格式
- README.md 中的链接描述同步更新：`Upgrade compatibility` → `Upgrade checklist`、`升级兼容` → `升级检查清单`
